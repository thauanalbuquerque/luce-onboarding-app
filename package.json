{
  "name": "luce-onboarding-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Onboarding multi-etapa de clínicas para a Luce (agente de IA no WhatsApp).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.2.0",
    "vite": "^6.0.7"
  }
}
